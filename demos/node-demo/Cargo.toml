[package]
name = "node-demo"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
assetify = { path = "../.." }
napi = { version = "3.12.2", features = ["async"] }
napi-derive = "3.6.3"

[build-dependencies]
napi-build = "2.4.1"
